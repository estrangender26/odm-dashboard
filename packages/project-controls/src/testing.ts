/**
 * Test-only entry point: fixtures shared between the package's own suites and
 * consumer test suites. Not part of the domain contract — production code must
 * import from "@lihok/project-controls" instead.
 */
export * from "./pr345SmokeFixture";
